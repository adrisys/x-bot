apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: x-bot-data
  namespace: x-bot
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
